platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine

params:
  VERSION:
  DEBUG:
  BRANCH:

inputs:
  - name: project-repo
  - name: image_dir

run:
  path: /bin/sh
  args: ["./project-repo/ci/tasks/generate-tag/task.sh"]